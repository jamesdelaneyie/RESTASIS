# Archive

Old sketches, unused helpers, and Arduino core stubs that **must not** sit next to the live sketch (they collide with `arduino:avr`).

| Path | What |
| --- | --- |
| `sketches/cylon.h` | Pre-shim LED chase |
| `sketches/scratch.h` | Motor experiments |
| `arduino-core-stubs/` | Copied `Arduino.h` / `pins_arduino.h` — Mega core provides the real ones |

Drop dated planning notes, extra `.ino`s, and gerber zips here or under `docs/notes/` and `hardware/`.
