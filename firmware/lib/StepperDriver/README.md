# 12-motor StepperDriver fork

Upstream: https://github.com/laurb9/StepperDriver (`MAX_MOTORS` is 3).

This copy extends `MultiDriver` / `SyncDriver` constructors and `rotate()`/`startMove()` through **12 motors** (STAEXE / RESTASIS, 2022). Do not replace with Library Manager.
